export interface TaskType {
    id: number,
    title: string,
    currentDay: string,
    category: string,
    priority: string,
    difficulty: number,
    endDay: string,
    status: boolean
}